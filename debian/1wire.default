# Defaults for 1wire initscript
# sourced by /etc/init.d/1wire
# installed at /etc/default/1wire by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the daemon(s).
SERVER_OPTS="--usb=all"
CLIENT_OPTS="-s localhost:4304"
