#! /bin/sh

rm -rf *cache

case `uname` in
    Darwin*) LTIZE=glibtoolize ;;
    *) LTIZE=libtoolize ;;
esac

$LTIZE -f -c -i && aclocal -I src/scripts/m4 && autoheader && autoconf && automake -a -c --foreign && echo "Run './configure' and 'make' to build owfs"
