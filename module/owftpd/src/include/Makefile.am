headerdir = ${prefix}/include
header_DATA = file_list.h ftp_command.h ftp_listener.h ftp_session.h oftpd.h telnet_session.h watchdog.h error.h af_portability.h daemon_assert.h

EXTRA_DIST = ${header_DATA}

clean-generic:

	rm -f *~ .*~
