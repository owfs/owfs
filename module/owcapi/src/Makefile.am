SUBDIRS = c include man

# don't want to add the example as a subdirectory right now...
EXTRA_DIST = example/owdir.c example/Makefile.example example/Makefile example/Makefile.in

clean-generic:

	@RM@ -f *~ .*~

